# streamlit e starlette ficam travados numa versão específica (em vez de
# ">=") por causa de um bug real que já derrubou o app inteiro em produção:
# versões recentes do Streamlit (>=1.6x) passaram a montar o próprio
# servidor web em cima do Starlette (streamlit/web/server/starlette/...),
# e o Streamlit chama a classe interna `GZipResponder` do Starlette com uma
# lista fixa de argumentos. O Starlette 1.4.0 acrescentou um novo argumento
# OBRIGATÓRIO (`thread_minimum_size`) nessa mesma classe - então, se o
# ambiente instala streamlit + starlette>=1.4.0 juntos, TODA requisição ao
# app quebra com "TypeError: GZipResponder.__init__() missing 1 required
# keyword-only argument: 'thread_minimum_size'" (é exatamente o erro "Oh
# no. Error running app." visto no Streamlit Community Cloud). O próprio
# streamlit já pede uma faixa mais estreita (starlette<1.4.0), mas isso
# depende de como cada ambiente resolve as versões - por segurança, fixamos
# aqui os dois numa combinação testada e confirmada sem esse bug.
streamlit==1.61.1
starlette==1.3.1
streamlit-authenticator==0.3.2
bcrypt>=4.0
pandas>=2.0
plotly>=5.22
chardet>=5.2
PyYAML>=6.0
requests>=2.31
# kaleido: rasteriza os gráficos Plotly como imagem pro relatório em PDF
# (core/pdf_report.py). A versão 0.2.1 (usada numa primeira versão deste
# recurso) tem um bug conhecido no Windows: quando o caminho do projeto tem
# espaço (ex.: "Vinicius Bemfica", "Métricas QA"), o subprocesso interno do
# kaleido falha com "--plotlyjs argument is not a valid URL or file path" -
# https://github.com/plotly/Kaleido/issues (Popen com shell=True no Windows
# quebra o path com espaço). kaleido>=1.0 reescreveu essa parte (usa
# choreographer, sem shell=True) e não tem esse problema. Em troca, não vem
# mais com um navegador embutido: se não achar um Chrome/Chromium/Edge/Brave
# já instalado no sistema, baixa um "Chrome for Testing" próprio na primeira
# vez que gera uma imagem (chamada explícita em core/pdf_report.py) e
# reaproveita esse download depois. No Streamlit Community Cloud, esse
# "Chrome for Testing" baixado sozinho falha ao abrir porque o container é
# minimalista e não tem as bibliotecas do sistema (libnss3, libgtk-3-0,
# libasound2 etc.) que QUALQUER Chrome/Chromium precisa pra rodar - por isso
# o arquivo packages.txt (raiz do projeto) instala o pacote "chromium" via
# apt: ele já traz essas bibliotecas junto (como dependência), e o
# kaleido passa a achar e usar esse chromium do sistema direto, sem
# precisar baixar nada.
kaleido>=1.0
reportlab>=4.0
# google-api-python-client / google-auth: usados só pela busca de arquivos
# .csv numa pasta compartilhada do Google Drive (core/google_drive_client.py
# → tela "Importar Dados" → "Buscar arquivo no Google Drive"). Autenticação
# via Conta de Serviço (sem tela de login interativa) - credenciais vêm do
# st.secrets["google_drive"] em produção (Streamlit Community Cloud) ou de
# core/google_drive_credentials.json localmente (nunca versionado - ver
# .gitignore).
google-api-python-client>=2.100
google-auth>=2.23